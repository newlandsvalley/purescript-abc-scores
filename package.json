{
  "private": true,
  "scripts": {
    "build": "pulp build",
    "example": "pulp build --main Example.Main --include example/src --to example/dist/app.js"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
